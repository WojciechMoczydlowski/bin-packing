# Wojciech Moczydlowski bin packing problem

class AlgorithmResult:

    def __init__(self, algorithm_type, items, boxes, time):
        self.algorithm_type = algorithm_type
        self.items = items
        self.boxes = boxes
        self.time = time
